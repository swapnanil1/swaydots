# ~/.config/sway/config
# Main Sway configuration file - Includes components from config.d/

# Include files from the config.d directory
# Order matters: Variables need to be defined before use.

# Variables, Applications, Colors
include config.d/00-variables

# Fonts, Borders, Gaps, Window Colors
include config.d/10-appearance

# Monitor setup (add input config here if needed)
include config.d/20-outputs-inputs

# Startup applications (except idle)
include config.d/30-autostart

# All keybindings and modes
include config.d/40-keybindings

# Swayidle configuration
include config.d/50-idle

# Optional: Include user-specific overrides or machine-specific settings last
# include config.d/99-local
